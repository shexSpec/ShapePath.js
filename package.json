{
  "name": "shape-path",
  "description": "A monorepo of typescript libraries and WebApps for parsing and executing ShapePaths",
  "private": true,
  "scripts": {
    "test": "cd packages/core && npm run test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/shexSpec/ShapePath.js.git"
  },
  "workspaces": [
    "packages/*"
  ]
}
